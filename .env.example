# Configuración del servidor
PORT=5001
NODE_ENV=development

# MongoDB
# Local (opcional)
MONGODB_URI=mongodb://localhost:27017/motorme
# O usar MongoDB Atlas (recomendado):
# Nota: si tu contraseña tiene caracteres especiales, codifícala en la URL (p.ej. * => %2A, @ => %40)
# MONGODB_URI=mongodb+srv://USUARIO:PASSWORD_CODIFICADO@HOST/motorme?retryWrites=true&w=majority&appName=MotorMe

# JWT
JWT_SECRET=tu_clave_secreta_super_segura_aqui
JWT_EXPIRE=7d

# Cloudinary (opcional - para almacenamiento de imágenes en la nube)
CLOUDINARY_CLOUD_NAME=tu_cloud_name
CLOUDINARY_API_KEY=tu_api_key
CLOUDINARY_API_SECRET=tu_api_secret

# Frontend URL (para CORS)
# Si usas Live Server de VSCode normalmente es 127.0.0.1:5500
FRONTEND_URL=http://127.0.0.1:5500
